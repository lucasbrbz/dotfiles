# i3blocks config file
#
# Please see man i3blocks for a complete reference!
# The man page is also hosted at http://vivien.github.io/i3blocks
#
# List of valid properties:
#
# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
#command=/usr/lib/i3blocks/$BLOCK_NAME
command=~/.config/i3blocks/blocks/$BLOCK_NAME
interval=10
separator_block_width=15
separator=true

# Media Player
[playerctl]
# label=
# label= 
interval=1

# [launcher]
# label=
# interval=once

# Wi-Fi Network
[wifi]
# label=
instance=wlp0s20f3
interval=1

# Wired Network (Ethernet)
[network]
label= 
instance=enp0s31f6

# Wired Network (USB)
[network]
label= 
instance=enx6c1ff7068326

# Bluetooth (shows when active)
[bluetooth]
#label=

# Audio
[audio]
label= 
interval=1

# Battery
[battery]
instance=0
urgent=15
interval=1

# Date
[date]
label=󰃭 
interval=60

# Time
[time]
label= 
interval=1

